@page
@model Crisan_Adriana_proiect.Pages.Toys.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Toy</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Toy.ID" />
            <div class="form-group">
                <label asp-for="Toy.Name" class="control-label"></label>
                <input asp-for="Toy.Name" class="form-control" />
                <span asp-validation-for="Toy.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Toy.Description" class="control-label"></label>
                <input asp-for="Toy.Description" class="form-control" />
                <span asp-validation-for="Toy.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Toy.Price" class="control-label"></label>
                <input asp-for="Toy.Price" class="form-control" />
                <span asp-validation-for="Toy.Price" class="text-danger"></span>
            </div>
            <label asp-for="Toy.SellerID" class="control-label">Publisher Name</label>
            <select asp-for="Toy.SellerID" class="form-control" asp-items="ViewBag.SellerID">
            </select>
            <span asp-validation-for="Toy.SellerID" class="text-danger"></span>
            <div class="form-group">
                <div class="table">
                    <table>
                        <tr>
                            @{
                                int cnt = 0;
                                foreach (var cat in Model.AssignedCategoryDataList)
                                {
                                    if (cnt++ % 3 == 0)
                                    {
                                    @:</tr><tr>
                                    }
                                    @:<td>
                                        <input type="checkbox"
                                               name="selectedCategories"
                                               value="@cat.CategoryID"
                                               @(Html.Raw(cat.Assigned ? "checked=\"checked\"" : "")) />
                                        @cat.CategoryID @: @cat.Name
                                    @:</td>
                                }
                            @:</tr>
                            }
                        </table>
                    </div>
                </div>
                <div class="form-group">
                    <input type="submit" value="Save" class="btn btn-primary" />
                </div>
            </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
